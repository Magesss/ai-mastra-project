{
  "name": "ai-mastra-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "mastra dev",
    "build": "mastra build && tsc src/worker.ts --outDir dist --module ES2022 --target ES2022 --moduleResolution node",
    "start": "mastra start",
    "deploy": "npm run build && wrangler deploy",
    "deploy:staging": "npm run build && wrangler deploy --env staging",
    "deploy:prod": "npm run build && wrangler deploy --env production"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "engines": {
    "node": ">=20.9.0"
  },
  "dependencies": {
    "@ai-sdk/openai": "^1.3.24",
    "@mastra/core": "^0.13.1",
    "@mastra/libsql": "^0.13.1",
    "@mastra/loggers": "^0.10.6",
    "@mastra/memory": "^0.12.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@mastra/deployer-cloudflare": "^0.11.5",
    "@types/node": "^24.2.1",
    "mastra": "^0.10.20",
    "typescript": "^5.9.2"
  }
}
